BUILD instructions
1. Clone the front end repo directly inside src/main/resources/static/react folder
	git clone https://github.com/TecOps/workflow-frontend.git . inside react folder would do the job

2. When you use mvn spring-boot:run in backend root folder application will automatically build the front too

